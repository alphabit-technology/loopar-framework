import BaseInput from './base-input.jsx';
import {FormLabel} from './FormLabel.jsx';

const invalidClass = {
  border: 'border-red-500/50',
  label: 'text-red-500/50',
}

export {
  BaseInput,
  FormLabel,
  invalidClass,
}